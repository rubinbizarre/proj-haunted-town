//move_speed = 2;
//move_speed_init = move_speed;

//if (instance_exists(obj_node_nev_home)) {
//	home_id = obj_node_nev_home;
//} else {
//	show_message("obj_nev_van CREATE:\nhome node not found");
//}

//my_path = path_add();

//current_state = "PARKED";
//path_set_kind(my_path, 0);

//target_x = 0;
//target_y = 0;

//scale_init = image_xscale;

//// when created, set x,y to home_id x,y
//x = home_id.x;
//y = home_id.y;

//check_timer = irandom(60); // stagger initial checks so npcs don't all think/execute logic at once
//check_interval = 60; // check routine every 1 sec at 60 fps

image_alpha = 0.3;